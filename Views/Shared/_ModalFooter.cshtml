@model ModalFooter
@{
}
<div class="modal-footer">
	<button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
	<button type="submit" class="btn btn-primary">@(!string.IsNullOrEmpty(Model.SubmitButtonText) ? Model.SubmitButtonText : "Submit")</button>
</div>